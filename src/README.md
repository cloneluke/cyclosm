# src/ - Shared libraries and configurations

This directory contains reusable packages and configurations shared across all applications.

## Directories

- **tile-schema/** - Planetiler configuration and cycling-specific tile schema
- **tile-provider/** - TypeScript tile source management and switching logic
- **style/** - MapLibre GL style definitions and layer configurations
